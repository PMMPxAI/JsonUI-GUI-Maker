{
  "name": "jsonui-gui-maker",
  "version": "2.0.0",
  "description": "Eclipse UI Forge — Visual Bedrock JSON UI Editor",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
